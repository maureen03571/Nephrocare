# Server
PORT=3001
NODE_ENV=development

# Google Sign-In (OAuth) — same Client ID as the frontend VITE_GOOGLE_CLIENT_ID
# Create at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=

# Gemini AI — add last when ready
# GEMINI_API_KEY=
