# Backend API URL (required for production — e.g. your Render backend URL)
# VITE_API_URL=https://nephrocare-api.onrender.com

# Google Sign-In — Web application Client ID from Google Cloud Console
# Must match backend GOOGLE_CLIENT_ID
VITE_GOOGLE_CLIENT_ID=
